import 'package:flutter/material.dart';

class AppColors {
  // Basic colors
  static const white = Colors.white;
  static const black = Colors.black;
  static const transparent = Colors.transparent;

  // Brand colors
  static const primaryColor = Color(0xFF00695c);
  static const pink = Colors.pink;
  static const purple = Colors.purple;
  static const greyColor = Color(0xFF9CA5B0);
  static const gradientDownColor = Color(0xFFC8D6DF);

  // Status colors
  static const blue = Colors.blue;
  static const red = Colors.red;
  static const green = Colors.green;
  static const orange = Colors.orange;
  static const yellow = Colors.yellow;

  // App specific colors
  static const Color positiveColor = Color(0xFF8CC73F);
  static const Color negativeColor = Color(0xFFFF6961);
  static const Color darkBackground = Color(0xFF1A1A2E);
  static const Color settingsBackground = Color(0xFF2D1B69);

  // Camera UI colors
  static const Color cameraOverlay = Color(
    0x99000000,
  ); // Black with 60% opacity
  static const Color recordingIndicator = Color(0xFFFF0000); // Red
}
